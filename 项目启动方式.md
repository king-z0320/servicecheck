# 项目启动方式

本文只说明本项目的本地启动和使用顺序，并明确区分“单通话质检”和“批量音频质检”。两种模式共用 PostgreSQL、质检 API、DeepSeek、RAG 和审计模拟服务；批量模式另外使用 Docker Redis、Outbox Publisher 和真实音频 Worker。

## 1. 两种启动模式

| 组件 | 单通话质检 | 批量音频质检 | 用途 |
|---|---:|---:|---|
| PostgreSQL 17 | 必需 | 必需 | 保存通话、运行、报告、批次、文件状态、Outbox 和检查点 |
| 审计模拟服务 | 必需 | 必需 | 提供 CRM/审计快照模拟接口，地址 `127.0.0.1:5002` |
| FastAPI 质检 API | 必需 | 必需 | 单通话分析、批次创建和状态查询，地址 `127.0.0.1:5001` |
| 静态页面服务 | 必需 | 可选 | 单通话工作台，地址 `127.0.0.1:8080` |
| Docker Redis | 不需要 | 必需 | Redis Streams、Consumer Group、Pending 和 ACK |
| Outbox Publisher | 不需要 | 必需 | 把 PostgreSQL Outbox 事件投递到 Redis Stream |
| 批量 Worker | 不需要 | 必需 | 读取真实音频并执行转码、ASR、情绪识别和质检 |

单通话工作台读取 PostgreSQL 中已有的通话和转写，再把转写提交给质检 API。它不是“上传一个音频并现场 ASR”的页面。批量模式才会从指定目录读取真实音频并执行完整音频处理。

## 2. 每个 PowerShell 窗口先激活项目环境

除 Docker 命令外，每个新 PowerShell 窗口都先执行：

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"
```

脚本会：

- 激活 Conda `servicecheck`；
- 设置 PostgreSQL、ArtifactStore、模型和缓存路径；
- 把当前目录切换到项目根目录；
- 确认实际解释器是 `D:\software\anaconda\envs\servicecheck\python.exe`。

DeepSeek 密钥保存在项目根目录的 `.env`，不要写入本文或提交到 Git。启动 API 和 Worker 前应确认 `.env` 已配置有效密钥。

## 3. 两种模式共用的启动准备

### 3.1 启动 PostgreSQL

重启电脑后 PostgreSQL 不会自动启动。打开 PowerShell：

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"

pg_ctl `
  -D "E:\客服质检agent项目\serviceCheck\data\postgres" `
  -l "E:\客服质检agent项目\serviceCheck\.runtime\postgres\postgres.log" `
  start
```

检查数据库：

```powershell
pg_isready -h 127.0.0.1 -p 55432
```

预期结果：

```text
127.0.0.1:55432 - accepting connections
```

如果提示 PostgreSQL 已经运行，不要重复启动。

PostgreSQL 程序和数据位置：

```text
程序：D:\software\anaconda\envs\servicecheck\Library\bin\postgres.exe
数据：E:\客服质检agent项目\serviceCheck\data\postgres
```

### 3.2 升级数据库 Schema

首次启动或拉取到新的 Alembic 迁移后执行：

```powershell
alembic upgrade head
alembic current
```

当前预期 revision 为：

```text
0003
```

日常代码和 Schema 都没有变化时，不需要重复执行迁移。

完成共享准备后，根据需要选择第 4 节的单通话模式或第 5 节的批量模式。不要同时启动两个 `api_server.py`，两种模式共用同一个 API 进程和 `5001` 端口。

## 4. 启动单通话质检

### 4.1 单通话处理流程

单通话工作台的正常流程是：

1. 页面从 PostgreSQL 读取案件、通话和已有转写；
2. 用户进入通话详情并点击“AI 质检”；
3. 页面调用 `POST /api/agent/analyze`；
4. API 执行事件提取、RAG、规则判断、审计查询、QualityGate 和必要的有限 Agent Loop；
5. 结果写入 PostgreSQL，并在页面展示分数、违规项、证据和处置建议。

单通话模式不需要 Redis、Publisher 或批量 Worker。需要三个独立 PowerShell 窗口。

### 4.2 终端 A：启动审计模拟服务

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"
python mock_audit_server.py
```

### 4.3 终端 B：启动 FastAPI 质检 API

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"
python api_server.py
```

API 启动时会加载本地 BGE 索引并初始化质检服务。看到模型初始化日志时应等待服务开始监听 `5001`，不要重复启动第二个 API。

### 4.4 终端 C：启动单通话页面

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"
python -m http.server 8080
```

浏览器访问：

```text
http://127.0.0.1:8080/催收质检.html
```

进入案件和通话详情后，点击“AI 质检”即可提交当前通话的已有转写。

### 4.5 查看单通话结果

单通话报告会随运行记录持久化到 PostgreSQL。可以从工作台查看历史运行，也可以通过接口读取：

```text
GET /api/calls/{call_id}/runs
GET /api/agent/runs/{run_id}
GET /api/reports/{report_id}
```

单通话模式当前没有独立的 JSON/CSV 文件导出命令；其权威结果是 API 响应和 PostgreSQL 中的不可变历史报告。

### 4.6 检查单通话服务

```powershell
pg_isready -h 127.0.0.1 -p 55432
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:5002/mock/calls/CALL-COMPLIANT-001/crm-summary
Invoke-RestMethod http://127.0.0.1:5001/api/health
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:8080/催收质检.html
```

接口文档地址：

```text
http://127.0.0.1:5001/docs
```

## 5. 启动批量音频质检

### 5.1 准备批量音频目录

API 和 Worker 默认共享以下音频根目录：

```text
E:\客服质检agent项目\serviceCheck\audio
```

该目录由 `BATCH_AUDIO_ROOT` 控制；未配置时使用项目 `audio/`。如需覆盖，API 和 Worker 必须使用同一个值。

每个批次必须使用音频根目录下面的一个子目录。例如：

```text
E:\客服质检agent项目\serviceCheck\audio\
└── incoming\
    └── batch-20260819-001\
        ├── call-001.m4a
        ├── call-002.wav
        └── call-003.mp3
```

提交 API 时只传相对于 `audio/` 的目录，并统一使用正斜杠：

```text
incoming/batch-20260819-001
```

目录扫描规则：

- 支持 `.m4a`、`.wav`、`.mp3`、`.flac`；
- 只扫描指定目录的第一层文件，不递归扫描更深的子目录；
- 不接受绝对路径、反斜杠、`.` 或 `..`；
- 一个目录中没有支持的音频时拒绝创建批次；
- 默认单批最多 1000 个文件，待处理队列默认最多 1000 个文件；
- 创建批次时冻结文件列表，并记录每个文件的大小和 SHA-256；
- 批次完成前不要移动、覆盖或修改源音频，否则 Worker 的快照校验会失败。

当前 `audio1.m4a` 和 `audio2.m4a` 位于音频根目录本身。正式批量接口不接受 `source_dir="."`。如需批量处理它们，应先复制到一个明确的批次子目录，再提交该相对子目录。

### 5.2 启动 Docker Redis

先启动 Docker Desktop，然后执行：

```powershell
docker compose -f "E:\客服质检agent项目\serviceCheck\compose.stage2.yml" up -d redis
```

验证容器和 Redis：

```powershell
docker compose -f "E:\客服质检agent项目\serviceCheck\compose.stage2.yml" ps
docker compose -f "E:\客服质检agent项目\serviceCheck\compose.stage2.yml" exec -T redis redis-cli ping
```

预期看到 `servicecheck-redis-1` 为 `healthy`，并返回 `PONG`。Docker Desktop 中的 `servicecheck` 是 Compose 分组，展开后才能看到 Redis 容器。

### 5.3 启动批量模式的四个进程

分别打开四个 PowerShell 窗口，每个窗口先运行项目激活脚本。

终端 A，启动审计模拟服务：

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"
python mock_audit_server.py
```

终端 B，启动 FastAPI 质检 API：

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"
python api_server.py
```

终端 C，启动 Outbox Publisher：

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"
python -m qc.batch.outbox_publisher
```

终端 D，启动唯一的批量 Worker：

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"
python -m qc.batch.worker
```

Worker 启动时会预热 FunASR、说话人分离和情绪模型。当前机器使用 CPU，模型已经落盘后的预热仍可能需要约一分钟；看到 Worker 仍在加载模型时应等待，不要重复启动第二个 Worker。

批量模式目前没有批量操作页面。创建批次、查询状态使用 PowerShell、Swagger 或其他 HTTP 客户端；单通话页面不是批量控制台。

### 5.4 提交批次并取得 `batch_id`

在另一个已激活项目环境的 PowerShell 中执行。把 `source_dir` 替换成第 5.1 节实际准备的相对子目录：

```powershell
$requestBody = @{
  source_dir = "incoming/batch-20260819-001"
} | ConvertTo-Json

$requestHeaders = @{
  "Idempotency-Key" = "manual-batch-20260819-001"
}

$accepted = Invoke-RestMethod `
  -Method Post `
  -Uri "http://127.0.0.1:5001/batches" `
  -Headers $requestHeaders `
  -ContentType "application/json" `
  -Body $requestBody

$accepted  第四步：查看返回结果
$batchId = $accepted.batch_id 第五步：把批次编号保存下来,后续可以直接复用
```

正常返回 HTTP `202`，示例：

```json
{
  "batch_id": "服务端生成的 UUID",
  "status": "QUEUED",
  "total": 3
}
```

`batch_id` 是后续查询和导出的唯一批次标识。推荐每次业务提交使用稳定且唯一的 `Idempotency-Key`：同一个 Key 和同一个请求会返回原批次；同一个 Key 改传其他目录会返回 `409`，防止误建重复批次。

### 5.5 批量质检的内部处理过程

提交成功后不需要再运行批量执行命令，异步链路会自动推进：

1. API 扫描目录，冻结文件清单、大小和 SHA-256；
2. PostgreSQL 在同一个事务中创建批次、文件记录和 Outbox 事件；
3. Publisher 从 PostgreSQL 读取 Outbox，写入 Redis Stream；
4. Worker 从 Redis Consumer Group 领取一个文件；
5. Worker 校验源文件仍与创建批次时的快照一致；
6. Worker 依次执行转码、ASR/说话人分离、情绪识别、事件提取、RAG、审计、QC 和必要的 Agent Loop；
7. 每个阶段的状态、次数、耗时、产物 URI、SHA-256 和生产版本写入 PostgreSQL；
8. 完成的阶段检查点会在中断重跑时复用，不重复执行已验证有效的阶段；
9. 最终报告或结构化失败写回批次文件记录，随后 Worker ACK Redis 消息；
10. 全部文件进入终态后，批次汇总为 `COMPLETED`、`PARTIAL` 或 `FAILED`。

当前是一个 Worker 进程，模型在该进程内只加载一次。单文件失败不会阻止同批其他文件继续处理；可重试错误只重试当前失败阶段，超过上限进入 `FAILED_FINAL` 并记录死信详情。

### 5.6 查询批次和文件状态

查询批次汇总：

```powershell
Invoke-RestMethod "http://127.0.0.1:5001/batches/$batchId" | ConvertTo-Json -Depth 10
```

查询每个文件、阶段、请求快照和结果快照：

```powershell
Invoke-RestMethod "http://127.0.0.1:5001/batches/$batchId/items" | ConvertTo-Json -Depth 20
```

批次状态含义：

| 状态 | 含义 |
|---|---|
| `QUEUED` | 已创建，仍有文件等待 Worker |
| `RUNNING` | 至少一个文件正在执行或等待中断恢复 |
| `COMPLETED` | 所有文件均为 `DONE` |
| `PARTIAL` | 没有最终失败，但至少一个文件需要人工复核 |
| `FAILED` | 至少一个文件进入 `FAILED_FINAL` |

文件常见状态包括 `PENDING`、`RUNNING`、`INTERRUPTED`、`DONE`、`HUMAN_REVIEW` 和 `FAILED_FINAL`。详细质检结果位于对应 item 的 `result_json`，阶段详情位于 `stages`。

### 5.7 手动导出批量报告

先确认批次已经进入 `COMPLETED`、`PARTIAL` 或 `FAILED`，然后在已激活 `servicecheck` 的终端执行：

```powershell
python -m qc.batch report $batchId
python -m qc.batch export $batchId exports
```

`report` 在终端输出快速进度、阶段平均耗时和吞吐摘要；完整的 `by_status` 和失败状态以第 5.6 节批次 API 为准。`export` 同时生成 JSON 和 CSV，默认保存到：

```text
E:\客服质检agent项目\serviceCheck\data\artifacts\exports\<batch_id>.json
E:\客服质检agent项目\serviceCheck\data\artifacts\exports\<batch_id>.csv
```

JSON 包含批次汇总、所有文件记录、阶段信息、请求快照和结果快照。CSV 适合汇总查看，列包括：

```text
callId, source_uri, status, score, disposition, failed_reason
```

导出目标参数 `exports` 是 `ARTIFACT_ROOT` 下的相对目录，不要传项目外绝对路径。重复执行相同导出命令会重新生成该批次的 JSON/CSV，并更新导出记录和 SHA-256。

正式异步批量只使用 API、Publisher 和 Worker。不要使用以下旧命令执行真实批量：

```text
python -m qc.batch ingest ...
python -m qc.batch run ...
python -m qc.batch resume ...
```

这些旧执行入口中的 `run/resume` 仍注入 `FakeAudioStageRunner`，只用于离线开发和历史兼容；只有 `report/export` 可以作为正式异步批量的查看和导出工具。

### 5.8 中断与续跑

- Worker 异常退出时，尚未 ACK 的 Redis 消息保留在 Pending；重启 Worker 后通过 `XAUTOCLAIM` 重新领取；
- Worker 校验并复用已经完成的阶段检查点，从最近有效阶段继续；
- Publisher 或 API 退出后，可以直接按第 5.3 节重启；
- 当前 Redis 关闭了 AOF 和 RDB，不应在仍有排队或 Pending 文件时停止、删除或重建 Redis 容器；
- Redis 数据整体丢失后的自动重建投递尚未闭环，不能把“Worker 续跑”理解为“Redis 数据丢失后也一定自动恢复”。

## 6. 不激活 Conda 时的等价写法

也可以使用 `conda run`：

```powershell
conda run -n servicecheck python mock_audit_server.py
conda run -n servicecheck python api_server.py
conda run -n servicecheck python -m qc.batch.outbox_publisher
conda run -n servicecheck python -m qc.batch.worker
conda run -n servicecheck python -m http.server 8080
```

两种方式不要混用：

- 已执行项目激活脚本：使用 `python ...`；
- 未激活：使用 `conda run -n servicecheck python ...`；
- 未激活时不要直接运行裸 `python`，否则可能调用错误的 Python 环境。

## 7. 正常停止

Python 服务分别在对应终端按 `Ctrl+C` 停止。批量模式应先确认所有批次进入终态，再停止 Worker 和 Publisher。

如需停止 Redis：

```powershell
docker compose -f "E:\客服质检agent项目\serviceCheck\compose.stage2.yml" stop redis
```

当前 Redis 没有持久化配置，不要在批次处理中执行 `down`、删除容器或重建容器。

确认不再需要数据库后，在已激活 `servicecheck` 的终端执行：

```powershell
pg_ctl `
  -D "E:\客服质检agent项目\serviceCheck\data\postgres" `
  stop -m fast
```

不要在任务管理器中强制结束 `postgres.exe`。`pg_ctl ... stop -m fast` 会让 PostgreSQL 正常终止会话并完成必要的落盘处理。
