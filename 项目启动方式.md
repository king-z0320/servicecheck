# 项目启动方式

本文说明当前“第一阶段：业务后端化”完成后的本地启动方式，包括一次性环境配置、PostgreSQL 启停、三个应用进程的启动顺序和常见检查命令。

## 1. 先说明：为什么新窗口又要设置环境变量

PowerShell 中通过 `$env:变量名 = 值` 设置的是**当前 PowerShell 进程的环境变量**。它只会被这个窗口以及从这个窗口启动的子进程继承。

另开一个独立 PowerShell 窗口时，新窗口不是旧窗口的子进程，因此不会自动得到旧窗口后来设置的变量。这是 Windows 的进程环境机制，不是项目或 Conda 的异常。

不建议用 `setx TEMP ...` 或修改 Windows 用户级 `TEMP/TMP` 来解决：那会影响电脑上的其他程序，让它们也把临时文件写入本项目。也不应把这段配置写入通常位于 C 盘的 PowerShell Profile，因为本项目明确禁止主动向 C 盘写项目配置。

本机的固定配置已经登记到 `servicecheck` Conda 环境，同时提供了项目内激活脚本。配置完成后：

- PowerShell 使用项目激活脚本时，这些变量会自动进入当前窗口；
- 使用 `conda run -n servicecheck ...` 时，命令会在带有这些变量的环境中运行；
- 不再需要每天复制整段 `$env:...`；
- 每个独立窗口只需执行一次项目激活脚本，因为不同窗口仍是不同进程。

## 2. 当前本地进程组成

当前阶段不使用 Docker Compose，也没有把 PostgreSQL 注册为 Windows 服务。完整本地运行由四个进程组成：

| 进程 | 地址或端口 | 用途 |
|---|---:|---|
| PostgreSQL 17 | `127.0.0.1:55432` | 保存案件、通话、运行、报告、事件和批量骨架数据 |
| 审计模拟服务 | `127.0.0.1:5002` | 为本地演示提供只读审计接口 |
| FastAPI 质检 API | `127.0.0.1:5001` | 业务后端和工作台接口 |
| 静态页面服务 | `127.0.0.1:8080` | 提供当前 HTML 工作台页面 |

PostgreSQL 程序来自：

```text
D:\software\anaconda\envs\servicecheck\Library\bin\postgres.exe
```

数据库数据保存在：

```text
E:\客服质检agent项目\serviceCheck\data\postgres
```

## 3. 已完成的一次性配置

本机 `servicecheck` Conda 环境已经持久化 15 个非秘密变量，包括临时目录、包缓存、模型目录、数据库地址、Artifact 目录、CORS 和 Python UTF-8 配置。

由于当前 Conda 24.11.3 在 PowerShell 7 中传递中文路径时存在编码兼容问题，**不要再直接复制长串 `conda env config vars set ...` 命令覆盖配置**。当前 Conda 状态已使用 ASCII JSON 加 Unicode 转义保存，能同时兼容 Conda 的 UTF-8 和 GBK 读取路径。

项目新增了经过真实验证的激活脚本：

```text
scripts\activate_servicecheck.ps1
```

它会完成以下操作：

1. 从脚本位置计算项目根目录，不依赖当前工作目录；
2. 在调用 Conda 前设置 E 盘临时目录、缓存目录和 UTF-8；
3. 必要时仅在当前 PowerShell 会话加载 Conda 模块，不写 C 盘 PowerShell Profile；
4. 激活 `servicecheck`；
5. 再次应用项目路径，避免 Conda 中的旧绝对路径覆盖当前项目位置；
6. 校验激活结果确实是 `servicecheck` 解释器。

以后每个新 PowerShell 窗口只需执行这一条：

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"
```

成功后会显示解释器、项目根目录和数据库地址。此时可以直接运行 `python`、`pg_ctl` 和 `alembic`。

### 3.1 密钥为什么不放进 Conda 环境

DeepSeek/OpenRouter 等密钥仍应保存在项目根目录的 `.env` 中，不要写进文档、命令历史或提交到 Git。

`api_server.py` 启动时会读取 `.env`。由于 `load_dotenv()` 默认不会覆盖已经存在的进程环境变量，因此：

- Conda 环境和项目激活脚本负责固定目录、数据库地址和 CORS；
- `.env` 负责本机密钥等敏感配置；
- Alembic 不主动读取 `.env`，但能直接读取 Conda 环境中持久化的 `DATABASE_URL`。

## 4. 每天启动项目

### 4.1 第一步：启动 PostgreSQL

重启电脑后 PostgreSQL 不会自动启动。打开一个 PowerShell：

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"

pg_ctl `
  -D "E:\客服质检agent项目\serviceCheck\data\postgres" `
  -l "E:\客服质检agent项目\serviceCheck\.runtime\postgres\postgres.log" `
  start
```

检查数据库是否可以接受连接：

```powershell
pg_isready -h 127.0.0.1 -p 55432
```

预期结果包含：

```text
127.0.0.1:55432 - accepting connections
```

如果提示已有 PostgreSQL 正在运行，不要重复启动，直接执行 `pg_isready` 检查即可。

### 4.2 第二步：升级数据库 Schema

首次部署或代码中新增了 Alembic 版本时执行：

```powershell
alembic upgrade head
```

它是幂等的；数据库已经处于最新版本时不会重复建表。日常没有代码升级时可以不执行。

查看当前数据库版本：

```powershell
alembic current
```

当前阶段预期 revision 为：

```text
0002
```

### 4.3 第三步：启动三个应用进程

每个服务会持续占用当前终端，因此分别打开三个 PowerShell 窗口。每个新窗口只需执行一次项目激活脚本，不再复制整段环境变量。

终端 A——审计模拟服务：

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"
python mock_audit_server.py
```

终端 B——FastAPI 质检 API：

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"
$env:HF_ENDPOINT = "https://hf-mirror.com"
python api_server.py
```

终端 C——静态页面：

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"
python -m http.server 8080
```

浏览器访问：

```text
http://127.0.0.1:8080/催收质检.html
```

### 4.4 不想激活 Conda 时

持久变量配置完成后，也可以继续使用 `conda run`。这种写法不需要先激活环境：

```powershell
conda run -n servicecheck python mock_audit_server.py
conda run -n servicecheck python api_server.py
conda run -n servicecheck python -m http.server 8080
```

两种方式不要混淆：

- 已执行项目激活脚本：使用简短的 `python ...`；
- 未激活：使用 `conda run -n servicecheck python ...`；
- 未激活时不要直接运行裸 `python`，否则可能调用错误的 Python 环境。

## 5. 启动成功检查

按顺序检查：

```powershell
pg_isready -h 127.0.0.1 -p 55432
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:5002/mock/calls/CALL-COMPLIANT-001/crm-summary
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:5001/api/health
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:8080/催收质检.html
```

其中 `CALL-COMPLIANT-001` 是当前模拟审计数据中已存在的演示通话。四条命令应分别显示 PostgreSQL 正在接受连接，以及三个 HTTP 请求返回成功状态。

FastAPI 接口文档通常可从以下地址查看：

```text
http://127.0.0.1:5001/docs
```

## 6. 正常停止

三个 Python 服务分别在对应终端按 `Ctrl+C` 停止。

确认不再需要数据库后，在已激活 `servicecheck` 的终端执行：

```powershell
pg_ctl `
  -D "E:\客服质检agent项目\serviceCheck\data\postgres" `
  stop -m fast
```

不要在任务管理器中强制结束 `postgres.exe`。`pg_ctl ... stop -m fast` 会让 PostgreSQL 正常终止会话并完成必要的落盘处理。

## 7. 常见问题

### 7.1 `python`、`pg_ctl` 或 `alembic` 找不到

先确认当前环境：

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"
python -c "import sys; print(sys.executable)"
```

预期解释器为：

```text
D:\software\anaconda\envs\servicecheck\python.exe
```

### 7.2 API 报 `DATABASE_URL is not configured`

重新运行项目激活脚本：

```powershell
. "E:\客服质检agent项目\serviceCheck\scripts\activate_servicecheck.ps1"
Write-Output $env:DATABASE_URL
```

预期输出为：

```text
postgresql+psycopg://servicecheck@127.0.0.1:55432/servicecheck
```

已经打开的旧窗口不会自动获得后来新增的环境变量，重新运行脚本即可刷新。

### 7.3 端口被占用

只读检查占用进程：

```powershell
Get-NetTCPConnection -State Listen -LocalPort 5001,5002,8080,55432 |
  Select-Object LocalAddress,LocalPort,OwningProcess
```

不要看到端口占用就直接结束进程。先根据 PID 核实它是不是本项目已经启动的服务。

### 7.4 是否能做到双击一次启动全部服务

技术上可以增加项目内的 `start_local.ps1`，由一个父进程设置环境并启动四个子进程；子进程会继承同一套配置。但仓库当前还没有这个启动器，本文件没有把它描述成已实现功能。

当前已经落地且最稳妥的简化方式是：固定变量已经持久化，日常每个新终端只执行一次 `activate_servicecheck.ps1`，然后运行对应入口。
