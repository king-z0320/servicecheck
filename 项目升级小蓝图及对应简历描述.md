# 客服质检 Agent 求职向项目升级小蓝图及对应简历描述

> 文档定位：这是从《项目工程化升级路线图.md》中筛出的“求职作品集最小充分版本”，目标不是模拟一套银行生产平台，而是用有限工作量证明 Agent 开发岗位真正关心的工程能力。
>
> 简历使用约束：本文末尾的简历描述是“升级完成后的候选稿”。对应功能、测试和演示证据完成前，不得复制到简历中当作已实现成果。

## 一、先纠正路线图中的两个问题

原路线图的技术方向大体合理，但直接全部实施并不适合当前目标。

第一，它混合了两类目标：一类是能证明 Agent 工程能力的核心能力，例如可恢复执行、工具约束、评测、证据治理；另一类是完整企业平台才需要的配套能力，例如五角色 RBAC、完整管理后台、多套仪表盘和正式备份体系。后者并非没有价值，但对大三求职项目的投入产出比较低。

第二，合并后的八个实施阶段不应平均投入。真正能拉开 Agent 开发求职差距的是一条可演示、可测试的纵向链路：

```text
真实音频任务
  -> 可靠异步执行与阶段恢复
  -> 有界 Agent 调用工具补充证据
  -> 确定性 QualityGate 兜底
  -> 不确定结果进入人工复核
  -> 严格数据集与可复现离线评测
  -> Trace、成本和故障证据可查询
```

如果所有阶段都只做半成品，面试价值通常低于把这条主线做深并留下可复现实验。

## 二、审查依据与当前已核实边界

### 2.1 本次筛选标准

一个升级项只有同时满足下列多数条件，才进入小蓝图：

1. 能解决当前源码中的真实缺口，而不是为了增加技术名词；
2. 能体现 Agent 特有的工程问题，例如非确定性、工具权限、循环失控、证据不足和评测退化；
3. 能通过离线测试、故障注入、评测报告或演示流程验证；
4. 面试时能讲清设计取舍、失败路径和边界；
5. 一台开发机可以复现，不依赖 Kubernetes、真实 CRM 或大规模数据。

### 2.2 从现有文档和源码核实的事实

- 当前已有 Flask 单通话 API、Pydantic 领域模型、DeepSeek 结构化事件提取、本地 RAG、确定性规则与 `QualityGate`；
- 当前已有 Direct 路径和有界 Planner/Executor/Evaluator Loop，并限制轮数、工具次数和运行时间；
- 当前已有 SQLite 运行记录、批次/文件/阶段状态、幂等摄取、恢复候选和 JSON/CSV 导出骨架；
- 当前批量入口仍注入 `FakeAudioStageRunner`，真实 `process_audio.py` 尚未接入批量链路；
- 当前批量恢复逻辑把“已完成阶段”仍交给实际 action 执行，尚未真正复用持久化产物；
- 当前批量管线可能把 `report=None` 按默认自动通过路径处理，并且重试次数可能记到错误阶段；
- 当前已有小规模金标回归与 RAG 命中测试，但还不是可比较模型、Prompt、知识和 Runtime 版本的完整评测机制。

### 2.3 本次没有核实的内容

本次任务只审查并生成文档，没有运行真实 DeepSeek、FunASR 或完整测试，因此本文不声称当前模型准确率、端到端时延、吞吐量或成本已经达到任何数值。

## 三、八个实施阶段的投入优先级

| 优先级 | 阶段 | 原因 |
|---|---|---|
| A：必须做深 | 0、2、4、6、8 | 分别证明正确性、可靠异步执行、可量化评测与可观测证据、Runtime 控制力和 Agent 权限安全，是项目的核心辨识度 |
| B：做最小闭环 | 1、3、5 | 为核心能力提供持久化、人工复核和可信 RAG，但不扩张成完整业务平台 |

原阶段 7“可观测性与成本账本”已并入阶段 4，形成“先采集运行证据，再据此评测和比较”的基础闭环。为便于与原路线图对照，后续阶段仍沿用原编号，因此编号中不再单列阶段 7。推荐按 `0 -> 1 -> 2 -> 3 -> 4 -> 5 -> 6 -> 8` 实施；优先级表示投入深度，不表示可以颠倒依赖顺序。

---

# 阶段 0：先修正批量骨架的正确性

## 审查结论

必须完整保留，而且应排在所有框架迁移之前。这一阶段表面上没有新技术栈，却最能体现是否真正理解状态机、失败关闭和恢复语义。当前骨架存在可由源码直接确认的正确性风险，如果带着这些问题接 Redis、PostgreSQL，只会把错误放大。

## 选择升级的点

### 0.1 用失败测试固定四类关键不变量

先新增能暴露旧问题的测试，再改生产代码：

- `AnalysisResult.status=FAILED` 或 `report=None` 时，批量文件只能进入失败终态，不能进入 `DONE`；
- 已标记成功的 ASR 阶段恢复时，ASR Runner 的调用次数必须保持不变；
- 某阶段失败时，错误码和 `attempts` 必须记录到该真实阶段；
- 同一文件或同一消息被重复处理时，只能有一份生效报告。

这组测试比单纯增加 happy path 更重要，因为它直接证明系统没有 Fail Open。

### 0.2 把“阶段状态”升级为“阶段产物检查点”

只保存 `ASR=DONE` 没有恢复价值。每个高成本阶段至少保存：

```text
stage / status / artifactUri / sha256 / producerVersion
startedAt / finishedAt / attempts / errorCode
```

建议的最小产物关系：

```text
TRANSCODE -> wav 路径与哈希
ASR       -> transcript 快照与 ASR 模型版本
QC        -> qcRunId、reportId 与版本清单
EXPORT    -> 导出文件路径与哈希
```

恢复时先校验产物存在、哈希匹配、版本可接受，再加载产物；校验失败才重新执行。这样“断点续跑”才不是重新跑一遍后修改状态字符串。

### 0.3 明确状态转换和条件更新

把合法转换集中定义，例如 `PENDING -> RUNNING -> DONE/HUMAN_REVIEW/FAILED_FINAL`，终态不得被普通更新覆盖。状态写入使用“当前状态仍为预期值”这一条件，更新失败说明存在重复消费或并发竞争，不能静默覆盖。

### 0.4 建立可重试错误分类

至少区分：文件损坏、输入校验失败、上游限流、网络超时、模型非法输出、未知内部错误。文件损坏和非法输入直接失败；限流和暂时网络错误有限重试；模型非法输出由 Gateway 做少量结构化重试，耗尽后转人工或失败。不能把所有异常都记成 QC 超时后统一重跑整条管线。

## 本阶段明确不做

- 不迁移 Web 框架；
- 不引入 Redis、PostgreSQL 或消息队列；
- 不追求真实模型压测。

先在现有 SQLite 和 Fake 依赖下证明语义正确，避免基础设施掩盖逻辑错误。

## 可验证完成标准

- 先看到新增测试在旧实现上按预期失败，再看到修复后通过；
- 用 Spy Runner 证明恢复时不会再次调用已完成 ASR；
- 对 `FAILED/report=None`、重复处理、真实阶段重试和无效产物分别有测试；
- 一次中断演示能够展示恢复前后的阶段记录和同一份产物哈希。

---

# 阶段 1：最小业务后端化，而不是重做完整管理系统

## 审查结论

保留 FastAPI、PostgreSQL 和迁移，但显著缩小范围。FastAPI 迁移本身不是 Agent 核心亮点，只是 Python AI 后端的求职基线；PostgreSQL 的价值在于后续 Outbox、唯一约束、乐观锁和版本查询，而不是为了把 SQLite 换成更响亮的名词。

## 选择升级的点

### 1.1 契约优先迁移到 FastAPI

先用现有 API 测试固定请求、响应、状态码和结构化错误，再把 Flask 路由中的业务调用抽到 Application Service，最后用 FastAPI 重新装配。保留 Pydantic 模型作为接口与领域边界，生成可检查的 OpenAPI Schema。

只把真正的网络 I/O 写成异步；FunASR、转码和批量质检进入 Worker。不要把同步数据库或阻塞 SDK 包在 `async def` 中伪装成异步。项目规模下可以先使用同步 SQLAlchemy Session，等有并发证据再决定是否引入 `AsyncSession/asyncpg`。

### 1.2 只建立支撑主链路的最小数据模型

第一版只迁移这些核心对象：

```text
qc_runs / qc_reports / batch_jobs / batch_items
stage_executions / outbox_events / review_tasks / eval_runs
```

报告必须保存模型、Prompt、规则、知识和 Runtime 版本。历史报告不可被新运行覆盖。规则正文、完整案件管理、坐席管理等暂时继续使用版本化文件或 Mock，不建设全套 CRM 数据模型。

### 1.3 用 Alembic 证明 Schema 可演进

至少保留“空库初始化”和“一次真实升级”两种迁移测试。面试时能展示新增字段如何兼容旧数据，比只提交最终建表 SQL 更能说明工程能力。

### 1.4 为音频和阶段产物提供存储抽象

定义 `ArtifactStore` 接口，默认实现使用项目目录内的受控本地存储，数据库只保存路径、哈希、类型和版本。当前求职版不强制部署 MinIO；只有在需要演示 S3 签名 URL 或多进程共享对象时再替换实现。抽象边界比盲目增加对象存储服务更有价值。

### 1.5 前端只做两个真实页面

保留简单任务详情页和报告详情页，数据全部从 API 读取，能看到任务阶段、证据引用、Agent 轨迹和错误。不重做完整 Vue 管理后台，不做大屏和规则配置中心。

## 本阶段明确不做

- 不做案件、坐席、规则、知识、报表的全套 CRUD；
- 不同时引入 MinIO、异步 ORM和复杂前端框架；
- 不为了“高并发”编造 Event Loop 或连接池收益。

## 可验证完成标准

- FastAPI 成为唯一 API 入口，原有接口契约测试保持通过；
- 空 PostgreSQL 可由 Alembic 初始化，旧 Schema 可迁移；
- 同一通话的多次运行和历史报告可分别查询；
- API 进程不直接执行转码、FunASR 或长批处理；
- 页面刷新后任务、报告和错误状态不丢失。

---

# 阶段 2：真实、可靠但不过度拆分的异步批量质检

## 审查结论

这是最高优先级阶段之一，应重点投入。保留 Redis Streams、Consumer Group、Outbox、幂等、重试和恢复；删掉 Audio/QC/Report 三类独立 Worker、SSE 和 100 文件性能承诺。一个 Worker 进程内按阶段执行就足以证明任务可靠性，后续只有出现资源隔离需求才拆分。

这里的 Worker 指独立于 FastAPI 的后台常驻进程：API 只校验请求、持久化任务并返回 `jobId`；Worker 从 Redis Streams 消费消息，领取任务后执行转码、ASR、质检和结果持久化，全部成功后再 ACK。它不是前端 Web Worker，也不等同于当前批量骨架中的 `ThreadPoolExecutor`。

求职版第一版只启动一个 Worker 服务，内部依次执行 `Audio -> QC -> Export` 阶段。Audio/QC/Report 是职责边界，不是必须部署的三个微服务；只有实际观察到 GPU 任务、LLM 网络调用或大文件导出互相争抢资源时，才按职责拆进程。FunASR 模型在 Worker 启动时加载一次并常驻，避免每个文件重复加载。

```text
POST /jobs
  -> PostgreSQL + Outbox
  -> Publisher 投递 Redis Streams
  -> Worker 消费并执行 Audio/QC/Export
  -> 保存阶段产物、状态和报告
  -> ACK 消息
```

## 选择升级的点

### 2.1 接通真实音频 Runner

把 `process_audio.py` 中经过验证的转码和 FunASR 能力封装为 `RealAudioStageRunner`，实现现有 Runner 协议。Worker 启动时加载一次模型并复用，不按文件重复初始化。自动测试继续使用 Fake；真实音频 E2E 只通过显式 marker 运行。

角色识别必须保留当前边界说明：单声道说话人聚类不能天然判断谁是坐席，不能把启发式映射写成准确角色识别。

### 2.2 建立 `202 + jobId` 的控制面接口

API 只校验请求、创建任务并返回 `202 Accepted`。客户端通过 `GET /jobs/{id}` 和 `GET /jobs/{id}/items` 查询进度。求职版使用轮询已经足够，不必为展示实时感额外维护 SSE 连接。

### 2.3 用 Transactional Outbox 消除“入库成功但消息丢失”

创建任务、创建批次文件和写入 Outbox 必须在同一 PostgreSQL 事务中完成。Publisher 把未发布事件投递到 Redis Streams 后标记发布。Publisher 崩溃可能导致重复消息，但不能导致任务永久丢失；重复问题由消费幂等解决。

这是很适合面试展开的工程点，因为它对应一个具体故障窗口，而不是泛泛地说“用了 Redis”。

### 2.4 按至少一次投递设计幂等消费者

Redis Streams 不提供天然的“业务恰好一次”。消费者应依靠任务唯一键、阶段条件更新和报告生效唯一约束做到业务幂等：

- 重复消息可以重复到达；
- 已完成阶段不重复执行；
- 同一 `runId` 只能有一份生效报告；
- ACK 只在数据库状态与产物持久化成功后发生。

简历中应写“幂等消费”或“降低重复执行风险”，不要写“实现严格 Exactly Once”。

### 2.5 有限重试、死信与背压

重试只从真实失败阶段开始，采用有上限的退避；耗尽后进入失败终态并保留错误上下文。使用独立 Semaphore 限制 ASR 和 LLM 并发，设置单批文件数、队列积压和单任务预算上限。一个坏文件不得阻塞同批其他文件。

### 2.6 做三种故障注入演示

至少准备：Worker 在 ASR 后退出、Redis 消息重复投递、LLM 返回 429。演示恢复后不重复 ASR、不生成重复报告、429 只重试 QC/LLM 阶段。这比声称“支持高并发”更可信。

## 本阶段明确不做

- 不上 Celery、RabbitMQ、Kafka 或 Kubernetes；
- 不拆 Audio/QC/Report 三套服务；
- 不做分布式 RPM/TPM 令牌桶；
- 不把本地测试文件数包装成生产吞吐指标。

## 可验证完成标准

- 一段真实音频能经 Worker 完成转码、ASR、Agent 质检和报告持久化；
- API 创建任务后不等待模型执行；
- 三种故障注入均有自动测试或可重复演示脚本；
- 重复投递只保留一份有效报告；
- Worker 重启后从有效检查点继续；
- 终态文件数始终等于批次文件总数。

---

# 阶段 3：最小可审计的 Human-in-the-loop 人工复核

## 审查结论

保留人工复核，但不建设企业审核中心，也不在本阶段把人工结果接入候选数据集或正式金标。Human-in-the-loop 是 Agent 系统面对不确定性时的重要落地能力，求职价值高于复杂权限和排班系统。目标是证明模型不会在证据不足时强行自动落地，人工可以确认、修改或退回结果，并且人工修订不会污染模型原始输出。

## 选择升级的点

### 3.1 由确定性条件自动创建复核任务

以下结果自动入队：`PARTIAL`、`HUMAN_REVIEW_REQUIRED`、Gate 拒绝、RAG 无有效支持、外部审计不完整。路由条件必须由代码和 Gate 决定，不能让 LLM 自己批准“无需复核”。Gate、RAG 和审计问题作为结构化 `routeReasons` 保存；同一 `runId` 只能有一个生效的 `review_task`，避免同一结果因多个问题或重复消息生成多份任务。

### 3.2 原始报告与审核修订分离

模型原始报告保持不可变。审核员确认或改判时创建新的 `review_revision`，记录审核动作、修改前后快照、结构化理由码、备注和时间；复核任务通过 `effectiveRevisionId` 指向当前生效修订，不覆盖原报告。理由码至少包括事件漏提、事件错提、规则误配、RAG 错引、证据错误、ASR 问题和处置错误。理由码只用于审核审计和问题定位，本阶段不据此生成候选数据或正式金标。

### 3.3 用乐观锁处理并发提交

求职版不需要完整租约和多人排班，但要有 `version` 字段或条件更新。两个请求同时修改同一任务时，后提交者收到冲突响应，不能静默覆盖。提交接口还应接受幂等键，避免网络重试生成两份修订。

### 3.4 明确人工中断、恢复与结果生效边界

复核任务使用独立状态，例如 `PENDING -> IN_REVIEW -> RESOLVED/RETURNED_FOR_RERUN`，不要与 `qc_run` 或批量文件状态混成一个字段。`HUMAN_REVIEW_REQUIRED` 报告在人工提交前只能作为待审核结果，不能成为最终业务结论。人工确认或改判后由 `effectiveRevisionId` 明确当前有效结果；退回重跑时创建关联的新 `qc_run`，保留旧运行、原始报告和审核记录。

### 3.5 只做一个聚焦的审核页面

页面并排展示转写、事件、规则、RAG 引用、原始报告和审核表单；支持确认、改判、退回重跑。无需做任务分配、班组统计和复杂大屏。

## 本阶段明确不做

- 本阶段不单独建设角色系统，审核权限由阶段 8 的统一 RBAC 承载；不做审核员绩效和复杂租约调度；
- 不做双人仲裁平台；有分歧时用管理员离线确认即可；
- 不建设 `feedback_candidates`、候选数据确认、金标导出和数据集版本管理；人工复核结果不自动进入阶段 4 的评测数据；
- 不把所有自动结果都送人工，避免复核流程失去意义。

## 可验证完成标准

- 所有需人工状态都会生成且只生成一个复核任务；
- 人工提交前，待审核报告不会成为最终业务结论；
- 并发提交测试证明不会丢失更新，重复请求不会生成多份修订；
- 原始报告、修订报告、当前有效结果和理由均可查询；
- 退回重跑会创建关联的新运行，不覆盖旧运行和审核历史；
- 从“模型不确定”到“人工确认或改判后结果生效”能完成一次端到端演示。

---

# 阶段 4：建立评测与可观测证据闭环

## 审查结论

本阶段合并原阶段 4“Agent 评测”和原阶段 7“可观测性与成本账本”。目标不是建设完整评测平台或运维中心，而是先建立一条可复现的证据链：一次运行用了什么配置、经过哪些关键步骤、输出是否正确、相比上一次改动了什么、失败发生在哪里、消耗了多少模型调用。

评测与可观测性职责不同但共享同一批关联信息：评测判断“结果对不对、版本是否改善”，Trace、结构化日志和 Metrics 解释“系统做了什么、为什么失败、资源消耗如何”。Trace 不能代替金标，运行日志也不能直接当成评测真值。

## 选择升级的点

### 4.1 建立严格、分层的评测样本格式

每个案例使用严格 Schema，至少包含案例 ID、输入转写、期望事件类型与证据 turn、期望规则集合、允许的处置结果、必需/禁止引用、案例来源和标签说明。案例划分为：

```text
dev：日常调试
regression：阻止已修问题复发
challenge：歧义、依赖失败、注入和边界案例
```

第一版案例契约至少固定为：

```text
caseId / split / source / labelNotes
input.callStartedAt / input.transcript[]
expected.events[{eventType, requiredTurnIds}]
expected.ruleIds / expected.allowedDispositions
expected.requiredContextIds / expected.relevantContextIds
expected.forbiddenContextIds / expected.referenceAnswerPoints
```

`requiredContextIds` 表示结论成立必不可少的文档或 Chunk，用于 Context Recall；`relevantContextIds` 表示召回后可提供有效支持的集合，用于 Context Precision；`referenceAnswerPoints` 是 Judge 检查答案相关性和完整性时可引用的人工要点，不要求写成唯一标准文案。Schema 至少校验案例 ID 唯一、split 枚举有效、turn 引用真实存在、时间包含时区、事件/规则 ID 合法，以及必需引用与禁止引用不能重叠。

当前不建设数据集发布平台，也不要求维护复杂的语义版本号。每次评测只需自动计算整个数据集和单案例内容哈希并写入结果；文件内容改变，哈希自然改变，就视为不同数据集快照。这样几乎不增加人工维护成本，同时避免修改案例后仍把前后分数当成同一数据集比较。

当前规模不必构造 Shadow/Holdout 等复杂集合；但不能长期只在同一组 dev 案例上调参后声称泛化效果。标准答案不能由被评测的同一个模型自动生成后未经人工核对就作为真值。

### 4.2 用最小 Manifest 同时记录实际配置与改动说明

每次评测生成机器可读的 `manifest.json`。不追求复杂格式，但至少同时回答两个问题：

1. 实际运行的是什么：代码提交或关键文件哈希、数据集哈希、模型与 Prompt、规则、知识库、检索配置及创建时间；
2. 本次准备验证什么改动：用 `changeSummary` 和结构化 `changes` 记录更新了知识库、RAG、Prompt、规则或 Runtime 的哪些部分、为什么改、预期改善什么。

最小内容可以是：

```text
evalRunId / createdAt / codeCommit-or-sourceHash / datasetHash
model / promptVersion / ruleVersion / knowledgeVersion / retrievalConfig
changeSummary / changes
```

只写“这次更新了 RAG”属于人工 Changelog，无法证明评测进程实际加载了哪份知识和配置，因此不能取代上述实际配置字段。阶段 6 完成后，再向 Manifest 增加 `toolSchemaVersion`、`agentArchitectureVersion` 和 `runtimeConfig`，用于区分工具契约、双 Agent 角色和 Runtime 配置。若工作区没有可用提交号，就保存关键源码文件哈希，不能留空后假装可追溯。

### 4.3 第一版只计算业务核心指标与 RAG 四项指标

业务结果先实现：

- 事件类型与证据 turn 的 Precision、Recall、F1；
- 违规规则集合 Exact Match；
- 处置结果正确性，歧义案例可以声明多个允许结果，但不允许把应失败或应转人工的结果自动判为通过。

RAG 第一版采用常见的四个评测维度，但必须明确各自的数据来源：

| 指标 | 回答的问题 | 第一版计算方式 |
|---|---|---|
| Context Precision | 召回内容中有多少真正相关，相关内容是否排在前面 | 根据案例标注的相关文档/Chunk ID 确定性计算；没有相关性标签时不伪造分数 |
| Context Recall | 标准答案需要的证据是否被召回完整 | 根据必需文档/Chunk 与实际召回集合确定性计算 |
| Faithfulness | 报告中的结论是否能由召回证据支持 | 先由代码检查引用存在、版本、生效时间、事件类型和规则关系，再由 Judge 评估难以结构化的语义支持程度 |
| Answer Relevancy | 最终回答是否针对当前质检问题，是否答非所问 | 由 Judge 按固定 Rubric 评分，并保存理由与证据引用 |

这四项借用常见 RAG 评测术语，但本项目的案例格式、Rubric 和实现方式必须写清，不能把自定义结果直接包装成与外部基准完全可比的“RAGAS 分数”。现有 `Hit@K/Recall@K` 可以作为 Context Recall 的辅助检索指标继续保留。

Agent 任务成功率、重复/非法工具调用、Checkpoint 恢复、取消、审批与双 Agent 评测路由等指标，等阶段 6 的 Runtime 语义稳定后再增加。Token 和费用继续记录原始账本，但本项目不把它们包装成阶段 6 已实现的 Runtime 预算。没有人工转写金标时不计算或宣传 ASR 准确率；样本量不足时不做 P99 或规模化时延结论。

### 4.4 生成版本 Diff 和失败案例包

评测产物至少包含 `manifest.json`、`metrics.json`、`failures.json` 和 `diff.md`。Diff 同时列出总体指标变化、新增失败、已修复失败和仍然失败，不能只展示一个上涨的总分。

每个失败案例保存案例 ID、输入哈希、期望、实际输出、检索结果与引用快照、Judge 结果、关联 `traceId/runId` 和结构化失败原因。当前支持按案例重新执行并查询双 Agent 的工具轨迹；本阶段不承诺固定模型响应后的确定性重放。

### 4.5 暂缓自动回归门禁

第一版先把数据集、指标、失败案例和版本 Diff 做可信，不立即用退出码阻断发布。原因不是回归门禁没有价值，而是当前尚无稳定基线，提前设阈值容易变成拍脑袋数字。完成至少两轮可重复评测并明确哪些错误属于绝对不可接受后，再增加本地门禁；未来硬门禁优先覆盖 Fail Open、已修 regression 案例复发和确定性安全边界，不直接用 Judge 的主观总分阻断发布。

### 4.6 用“确定性硬判断 + LLM Judge 软判断”融合评分

融合不采用一个不透明的加权总分，而使用分层判定：

1. 代码先判断硬事实：事件/turn、规则集合、处置结果、引用是否存在、文档版本与有效期、事件类型、规则关系和分数复算；
2. 只有通过必要输入校验的案例才交给 Judge 判断语义层面的 Faithfulness、Answer Relevancy、解释完整性和证据是否真正支持结论；
3. 确定性检查失败时，Judge 的高分不能覆盖失败；确定性检查通过但 Judge 低分时，标记为 `NEEDS_REVIEW`，进入失败案例包供人工抽查；
4. 最终报告分别展示硬指标和 Judge 指标。可以额外给出仅用于观察的综合分，但必须公开权重，且不能作为 QualityGate 或自动处罚依据。

Judge 必须输出结构化结果，例如 `dimension/score/reason/evidenceIds/confidence`，并保存 Judge 模型、Prompt/Rubric 版本、原始输出哈希和调用 Token。Judge Prompt 要求只能引用提供的证据 ID，不能引用外部知识。应准备少量人工复核样本，检查 Judge 与人工的一致性；同一个待评模型自评的结果不得包装成客观准确率。

建议重点检查四种组合：

| 代码硬判断 | Judge | 处理方式 |
|---|---|---|
| 通过 | 通过 | 评测通过 |
| 通过 | 不通过 | 标记语义质量问题并人工抽查 |
| 不通过 | 通过 | 仍然失败，Judge 不得覆盖硬规则 |
| 不通过 | 不通过 | 失败并合并两侧原因 |

### 4.7 贯通统一关联标识

API、Outbox、Redis 消息、Worker、Agent Step、评测结果和报告统一携带 `traceId/runId/batchId/itemId`。`runId` 是长期业务查询键，`traceId` 表示一次技术执行链；Worker 恢复或重新消费时仍通过 `runId` 关联前后 Trace。评测失败案例保存这两个 ID，从错误结果可反查完整执行过程。

### 4.8 用 OpenTelemetry 记录关键链路 Span

OpenTelemetry 负责创建和传播 Trace/Span，并把数据导出到 Console、测试内存 Exporter 或轻量 Trace 后端。它不是日志数据库，也不会自动理解知识版本、Gate 原因等业务字段，这些字段仍需代码显式记录。

只覆盖关键业务链路，不给每个函数都埋 Span：

```text
create_job -> queue_wait -> transcode -> asr -> event_extract
           -> rag -> tool_call -> gate -> persist_report
```

不同 Span 记录的属性必须不同，不能机械套用同一组字段：

| Span | 应记录的核心属性 |
|---|---|
| `create_job` | `batchId`、文件数、Outbox 事件 ID、创建状态和耗时 |
| `queue_wait` | Stream/Consumer Group、投递次数、领取方式和 `queueWaitMs` |
| `transcode` | 输入格式、音频时长、Runner 版本、attempt、状态和耗时 |
| `asr` | ASR 模型版本、音频时长、attempt、状态、耗时和转写哈希 |
| `event_extract` / `llm_call` | provider、model、operation、Prompt 版本、attempt、latency、input/output tokens、tokenSource、Schema 校验结果和错误码 |
| `rag` | knowledgeVersion、retrievalConfig、Top-K、过滤条件、候选数、有效候选数、引用 ID/哈希和耗时 |
| `tool_call` | 工具名/版本、参数哈希、授权结果、timeout、attempt、结果状态和错误码；阶段 6 再补 capability、幂等键与审批信息 |
| `gate` | Gate/规则版本、通过与否、结构化理由码和最终路由 |
| `persist_report` | reportId、持久化状态、数据库 attempt 和耗时 |

每次真实 LLM 请求使用独立子 Span，因此失败重试产生多条 `llm_call`，每条都有自己的 attempt、耗时和 Token；不能只记录最终成功请求。Redis 消息传递标准 Trace Context，恢复或重复投递无法延续同一父子链时使用 Span Link，并始终保留 `runId` 关联。

Span 属性禁止保存完整 Prompt、完整转写、完整知识 Chunk、客户信息、API Key 和原始敏感工具参数。可改存版本号、长度、数量、结构化状态和哈希。

### 4.9 建立结构化日志系统

Trace 适合查看一次调用链，日志适合记录某个时刻发生的具体事件。日志统一输出 JSON，至少包含 `timestamp/level/service/traceId/runId/batchId/itemId/stage/status/attempt/durationMs/errorCode`；涉及模型时再增加 `provider/model/operation/tokenSource`。

重试、状态转换、错误分类、Gate 拒绝、Judge 失败和 Trace 导出失败都使用稳定事件名和错误码，不能只记录自然语言异常。Trace/日志导出失败不得让质检主链路随之失败，但应通过内部计数或降级日志暴露可观测数据丢失。

### 4.10 建立 Token 与成本用量账本

所有模型调用经 Gateway 写入独立用量记录：`invocationId/traceId/runId/provider/model/operation/attempt/inputTokens/outputTokens/tokenSource/latency/priceConfigVersion/estimatedCost`。失败重试也可能产生 Token 和费用，因此每次请求分别记账，并用唯一 `invocationId` 防止重复消费时重复记账。

Token 优先使用 Provider 返回值；Provider 未返回时记录 `unknown`，若以后增加本地估算则标记 `locally_estimated`，不能伪装成真实值。费用由带版本的价格配置估算并明确标记 `estimated`，不得表述为 Provider 实际账单。

### 4.11 暴露少量可解释的 Metrics

第一版保留：队列深度与最老消息年龄、各阶段成功/失败数、阶段耗时 Histogram、LLM timeout/429/非法输出数、Gate 拒绝数、Judge 调用失败数和人工复核数。阶段 6 完成后再增加重复/未知/越权工具、Checkpoint 恢复、取消请求、双 Agent 路由和审批结果等 Agent Runtime 指标；不增加本阶段未实现的 Lease 争抢或全局 Deadline 指标。

Counter 用于累计事件数，Gauge 用于当前队列深度等可升降数值，Histogram 用于耗时分布。Metric 标签只使用 `stage/status/errorCode/model/operation` 等低基数字段，禁止把 `runId/caseId/手机号/完整错误信息` 放进标签。单次运行详情通过 Trace 和日志查询，不用 Metric 承担。

### 4.12 建立日志、Trace 和评测产物的脱敏测试

对 Authorization、API Key、Cookie、手机号、完整 Prompt、完整转写、客户信息和敏感工具输出建立字段白名单、掩码或哈希策略。测试构造带已知敏感标记的数据，让其经过普通日志、异常日志、Span、用量记录、Judge 和失败案例导出，最后扫描全部序列化结果；敏感原文存在时测试失败，同时断言 `traceId/runId/errorCode/promptVersion/transcriptHash` 等必要字段没有被误删。

## 本阶段明确不做

- 不开发完整评测 Web 平台；
- 第一版不做自动回归发布门禁，不预设没有基线支撑的漂亮阈值；
- 不用同一个大模型的自评结果包装成客观准确率，不允许 Judge 覆盖确定性失败或替代 `QualityGate`；
- 默认离线确定性评测不调用付费模型或自动下载真实模型；Judge 评测必须显式启用，并单独记录调用与成本；
- 不用被 skip、中断或未运行的用例充当通过。
- 不制作多套 Grafana 大盘，不在缺少长期流量时定义可用性 SLO；
- 不用单机少量样本宣称 P95/P99 或生产容量；
- 不把完整用户内容写入日志、Span 或失败案例来换取调试方便。

## 可验证完成标准

- 严格 Schema 能拒绝缺少标准答案、引用标签或来源的非法案例，dev/regression/challenge 可分别运行；
- 每次运行自动生成数据集哈希、最小 Manifest、核心指标、失败案例和版本 Diff，所有指标都有数据来源和计算代码；
- 两个 Prompt 或 RAG 版本可生成差异报告，并能定位新增失败、已修复失败和仍然失败的案例；
- RAG 四项指标分别标明确定性或 Judge 数据来源，确定性失败不会被 Judge 高分覆盖；
- 输入一个 `runId` 可以定位队列、ASR、RAG、LLM、工具和 Gate 的关键 Span，并关联评测失败案例；
- LLM 429、工具超时、Schema 非法和 Judge 失败能从结构化日志中区分；
- 单 run 与单批次可以汇总模型请求数、已知 Token 和估算成本，用量未知时明确显示未知；
- 敏感字段扫描测试覆盖日志、Trace、Judge 和评测产物并通过；
- Trace 或日志导出后端不可用时，主业务和离线评测仍能完成，并留下可观测数据降级信号。

---

# 阶段 5：以评测收益为门槛的 RAG 工程化

## 审查结论

保留知识版本、混合检索、证据快照和发布回滚；不为了关键词强制上 pgvector、Cross-Encoder、多路召回和 Query Rewrite。当前知识量较小时，检索算法复杂度不是主要矛盾，证据能否追溯、有效期是否正确、升级是否经过评测更重要。

## 选择升级的点

### 5.1 建立不可变知识版本和构建清单

每次构建记录原文哈希、Chunk 策略、Embedding 模型、元数据 Schema 和索引哈希。发布版本不可原地修改，当前版本通过一个显式指针选择；回滚只是切回已构建版本，不临时重建历史索引。

求职版可以继续使用本地索引。只有当需要数据库过滤、并发查询或知识规模明显增长时，再把向量存储替换为 PostgreSQL + pgvector。

### 5.2 增加 Dense + BM25 的最小混合检索

现有语义检索适合相似表达，BM25 对规则编号、固定术语和专有词更稳定。采用简单的 Reciprocal Rank Fusion 合并结果，并在评测集上与 Dense-only 对比。只有 `Recall@K` 或错误引用指标确有改善才保留混合检索。

### 5.3 元数据过滤必须先于排序且不可绕过

事件类型、规则关系、生效时间和文档状态属于硬过滤，不应由相似度或 Query Rewrite 决定。过期规则、事件类型不匹配或未发布文档即使分数高也不能成为处罚依据。

### 5.4 保存历史证据快照

每条引用保存：

```text
documentId / documentVersion / chunkId / sourceRange
retrievalScore / rank / knowledgeVersion / contentHash
```

报告详情读取当时快照，而不是对当前知识库重新检索。这样才能解释“为什么当时给出这个结论”，也能准确比较知识版本变化。

### 5.5 把知识内容视为不可信输入

知识文档中的“忽略规则、调用工具”等文字只能作为被检索内容，不能改变 System Prompt、工具权限和 Gate。增加 Prompt Injection 挑战案例，验证恶意 Chunk 不会触发未授权工具或跳过人工复核。

### 5.6 Reranker 作为有证据的可选优化

只有混合检索仍存在候选排序问题，并且固定评测集证明 reranker 改善目标指标且成本可接受时，才引入一个 Cross-Encoder；否则不做。动态 Top-K、Query Rewrite 和多路召回不进入本轮小蓝图。

## 本阶段明确不做

- 不同时维护 pgvector、Milvus、Elasticsearch 等多套检索组件；
- 不因为模型更复杂就默认效果更好；
- 不把相似度分数当成违规概率；
- 不让新知识版本改变历史报告引用。

## 可验证完成标准

- 两个知识版本可构建、发布、切换和回滚；
- 历史报告在切换知识版本后仍显示原证据；
- Dense-only 与 Hybrid 使用同一评测集生成对照结果；
- 过期、错事件类型和注入文档不能支持自动处罚；
- 没有合格证据时结果稳定进入人工复核。

---

# 阶段 6：把有限 Loop 做成可恢复、可约束、可观测的双 Agent Runtime

## 审查结论

阶段 6 的目标不是把项目扩展成“很多 Agent”，而是把当前一次进程内的有界 Loop 升级为一个有持久状态、工具边界和人工接管能力的执行 Runtime。本阶段采用固定职责的双 Agent：一个负责基于证据提出和执行下一步动作，另一个负责独立检查报告是否被证据支持。两者共享同一个持久化 `AgentState`，但不能直接互相修改对方的状态或权限。

这和多 Agent 群聊不同：没有自动生成角色、没有无限自主规划，也不让两个模型互相讨论后直接决定业务结果。所有状态变更都由 Runtime 的确定性 reducer 应用，最终报告仍必须通过 `QualityGate`。本阶段暂不做运行租约、全局 Deadline、确定性重放、Checkpoint 自动迁移和独立的失控测试平台；这些边界必须在简历和面试中如实说明。

## 选择升级的点

### 6.1 将隐式循环重构为显式状态图，并引入双 Agent

Runtime 使用显式状态图，而不是让两个 Agent 自己维护一套循环：

```text
ROUTE
  -> EXEC_PLAN（执行 Agent 规划）
  -> AUTHORIZE
  -> ACT
  -> OBSERVE
  -> EVALUATE（评测 Agent 评估）
  -> REPLAN / GATE / WAITING_APPROVAL / HUMAN_REVIEW
  -> FINALIZE
```

这里的“执行 Agent”是当前 `PLAN -> ACT -> OBSERVE` 的 ReAct 风格执行者，负责根据证据缺口提出一个工具请求；“评测 Agent”只读取当前报告、证据、工具 Observation 和确定性 Gate 结果，输出结构化评测结论，例如 `PASS`、`NEEDS_MORE_EVIDENCE`、`REVISION_REQUIRED` 或 `HUMAN_REVIEW_REQUIRED`。评测 Agent 可以建议下一步，但不能直接调用业务写工具、修改报告、改变权限或宣布最终通过。

两类 Agent 不各自保存一份隐藏历史，而是共同使用类型化的 `AgentState`。至少包含：

```text
runId / stateVersion / currentNode / activeAgent
plan / reportSnapshot / evidenceBundle / observations
evaluatorDecision / roundCount / duplicateCallFingerprints
cancelRequested / pendingApproval / contextManifest
```

Agent 只返回经过 Schema 校验的 `ActionProposal` 或 `EvaluationDecision`；Runtime reducer 才能写入 `AgentState`。`FINALIZE` 是通过确定性条件后的状态转换，不是 LLM 工具；`REVISE_REPORT` 仍然只能执行现有的结构化、有限修订，不能让模型自由重写报告或修改扣分。

显式状态的作用是让“当前做到哪一步、谁可以做下一步、进程中断后从哪里继续、评测 Agent 是否试图越权”都有一个可查询的落点。实现可以继续使用手写状态机，不需要为了框架名称迁移到 LangGraph。

### 6.2 建立类型化 Tool Registry，并补足有业务意义的工具

每个工具声明：

```text
name / version / inputSchema / outputSchema
ownerAgent / readOnly / requiredCapability
timeout / retryPolicy / idempotencyPolicy
dataScope / riskLevel
```

模型只能输出注册工具名和通过 Pydantic 校验的参数。Runtime 在真正调用前检查工具是否存在、参数是否有额外字段、资源是否属于当前案件、当前 Agent 是否拥有该工具的 capability，以及工具输出是否符合 Schema。未知工具、参数走私和越权数据范围必须在执行前拒绝；绝不把模型自然语言拼成 SQL、Shell、任意 URL 或文件路径。

当前 Loop 的实际动作主要是 `EXPAND_CONTEXT`、`SEARCH_KNOWLEDGE`、`QUERY_ACTION_AUDIT` 和受限的 `REVISE_REPORT`，对现有质检闭环够用，但对展示 Agent 的工具编排能力偏窄。这里不是追求工具数量，而是补充能解决不同证据缺口、且权限边界清楚的能力：

1. 保留并注册 `expand_transcript_context`、`search_knowledge`、`query_action_audit`；把当前动作名和参数转换为稳定的输入/输出 Schema。
2. 新增只读的 `get_rule_detail(ruleId, effectiveAt)`，直接返回版本化规则及适用条件，避免所有规则问题都依赖模糊的语义检索。
3. 如果现有审核数据能提供严格的案件范围和脱敏结果，再增加 `query_review_history`；它只能返回同案件或聚合后的审核理由，不能把其他客户的原始内容塞进上下文。
4. 为 `6.7/6.8` 准备可查询的 Fake `create_follow_up_ticket` 与 `get_operation_status`。前者是副作用工具，后者用于超时后的结果确认；它们不能访问真实 CRM。

`FINALIZE` 是状态转换，不注册为工具；`REVISE_REPORT` 由 Runtime 调用确定性 reducer，不开放一个可以自由写入 `QualityReport` 的通用工具。工具注册表的目的不是让 Agent 看起来“工具很多”，而是使每个动作都能回答：谁能调用、能访问什么、失败如何处理、是否会产生副作用。

### 6.3 持久化 Step 与 Checkpoint；本阶段暂不引入 Lease

每个 Run 至少保存：当前节点、活动 Agent、结构化计划、工具参数摘要、工具 Observation 摘要、评测结论、报告快照、上下文清单、轮数和重复调用指纹。工具结果和对应的新状态必须在同一事务中提交；只写入 Observation 而没有推进状态，或只推进状态而丢失 Observation，都视为 Checkpoint 失败。

进程在一个工具步骤后退出时，恢复流程从最近一次已提交的 Checkpoint 继续，不把已经完成的工具结果再次发给模型。副作用工具如果在提交前发生未知中断，不能假定“没有执行”，必须按 `6.8` 的未知结果协议处理。

本阶段不做运行租约。执行模型暂定为“一个 Run 只有一个活动 Runtime Worker”，并用 `stateVersion` 条件更新拒绝旧状态覆盖新状态；这可以防止陈旧 Checkpoint 写回，但不能宣称已经解决多 Worker 并发推进问题。未来需要多 Worker 接管时，再单独增加 Lease 或队列级所有权协议。

### 6.4 增加取消传播和优雅中止；本阶段不增加全局 Deadline

保留现有单次 LLM/HTTP/工具超时，并增加 Run 级 `cancelRequested` 和可传递的 cancellation token：

```text
用户/批次/服务发出取消
  -> Runtime 标记 cancelRequested
  -> 当前节点在安全边界感知取消
  -> 不再启动新的 Agent 或工具动作
  -> 进入运行态 `CANCELLED` 或 `HUMAN_REVIEW_REQUIRED`
```

正在执行的只读调用可以等待其自身超时或返回；已经发出的副作用不能被伪装成“从未执行”，仍要记录未知状态。服务关闭时先停止领取新 Run，再让当前节点完成安全收尾。若公共 `ExecutionStatus` 目前没有 `CANCELLED`，应显式扩展状态枚举和持久化约束；不能把用户取消伪装成 `COMPLETED`。

本阶段接受 Agent 运行时间较长的业务假设，不引入跨步骤的绝对 `deadlineAt`，也不把“总时长”作为新的 Runtime 终止条件。代价是：如果外部依赖一直不返回，系统仍依赖单次调用超时和人工取消；这不是本阶段已经解决的全局时限问题，不能在简历中宣称“整体执行有界终止”。

### 6.5 提高有界轮数，增加重复调用检测与终止

当前循环的轮数上限可以适当提高，但仍必须是配置化硬上限，不能改成无限 Agent。上限应在离线评测集上观察“新增证据收益”后确定，而不是为了让更多案例通过而随意调大；现有工具调用上限继续作为兜底。

每次工具请求对 `toolName + 规范化参数` 计算指纹，并记录最近一次 Observation 的摘要。相同工具和相同参数在没有新增证据时再次出现，达到配置阈值后立即停止循环，转人工复核或失败关闭；不在本阶段实现复杂的语义等价计划判定。

本阶段不新增 Token、费用、全局时间和外部依赖熔断等多维预算控制。Gateway 仍可记录 Token/费用来源，但这些账本不被包装成已经实现的 Runtime 预算。Runtime 只强制执行：轮数上限、现有工具调用上限和重复调用终止。

### 6.6 建立可解释、可展示的上下文工程

上下文不再是每轮把完整通话和完整历史重新拼接给模型，而是按来源和风险分区：

```text
System Policy（不可被数据覆盖）
任务输入与案件卡
有效证据窗 / 规则引用 / RAG 命中
工具 Observation 摘要
历史决策摘要
当前轮数与剩余允许轮数
```

每个片段记录 `source/type/version/tokenCount/required` 等元数据。系统规则、授权信息、关键 Turn、规则编号和引用内容属于不可裁剪区；重复观察、低价值闲聊和已解决的历史理由可以按确定性策略摘要或裁剪。这样做的作用是控制上下文成本、减少无关信息干扰，并让 UI 能解释“这一轮 Agent 看到了哪些证据”。

本项目仍然只保留 Run 内工作记忆，不虚构跨 Run 的人格记忆。人工修订作为审核历史查询，不自动写回 Prompt，也不直接改变评测数据。

### 6.7 增加人工 Interrupt/Resume 语义

高风险副作用、证据冲突或需要人工确认的动作将 Run 持久化为 `WAITING_APPROVAL`，保存待执行工具、参数摘要、风险说明和过期时间。审批令牌绑定 `runId + tool + 参数哈希 + 审批人`，只能消费一次；批准后从原 Checkpoint 恢复，拒绝后该动作不能在同一 Run 中继续执行。

人工审批和人工质检改判是两件事：前者决定 Agent 是否可以执行一个高风险动作，后者决定质检报告是否生效。两者都必须写入 Trace 和审核记录，不能通过新建一个相同输入的 Run 静默绕过既有拒绝策略。真实 CRM 写接口不在本阶段范围内，用可查询 Fake Tool 验证协议即可。

### 6.8 定义副作用工具的执行协议

只读工具可以在 Registry、权限和轮数约束内自动执行；副作用工具必须具备幂等键、审批前置、调用前后审计和明确的 `UNKNOWN` 结果状态。请求超时不能直接当作“未执行”：Runtime 应先使用 `get_operation_status(operationKey)` 查询，只有确认未执行时才允许按策略重试；无法确认时转人工。

这个协议的价值是避免“第一次写入其实成功、第二次重试又写一遍”。求职版只需用状态可查询的 Fake Tool 演示，不得把 Fake 接口写成已经对接 CRM。

### 6.9 保持现有 Gateway，本阶段不再扩展

当前代码级 LLM Gateway 已足以承担模型调用统一入口。本阶段不拆独立微服务、不增加多 Provider 自动降级，也不重复建设一套新的重试/Token 账本。双 Agent 继续共用现有 Gateway，但使用不同的 system prompt、输入 Schema、输出 Schema 和 capability；调用结果通过 `runId/stepId/agentRole` 进入现有 Trace 和用量记录。

### 6.10 固定双 Agent 的权限与权力边界

双 Agent 的差异不是名字，而是权限和可写范围不同：

| 组件 | 可以做什么 | 明确不能做什么 |
|---|---|---|
| 执行 Agent | 规划下一步、申请并调用已注册的证据查询工具、提出副作用请求 | 修改预算/轮数上限、修改权限、直接改最终分数、直接执行未经审批的副作用 |
| 评测 Agent | 读取状态快照、证据包和 Gate 结果，判断支持是否充分，提出 `REPLAN` 或人工建议 | 调用业务写工具、修改 `QualityReport`、增加轮数、改变 capability、覆盖 Gate 失败 |
| Runtime/Reducer | 校验并应用状态转换、扣减轮数、执行受限修订、路由人工 | 接受模型自然语言作为状态更新 |
| QualityGate | 确定性检查证据、规则、生效期、分数和最终处置 | 被任一 Agent 的高分或 `PASS` 覆盖 |

评测 Agent 可以拥有独立的评测域只读能力，例如 `read_state_snapshot`、`check_evidence_binding` 和 `preview_rule_application`；这些能力只能生成评测结果或预览，不能写回报告。执行 Agent 拥有 `expand_transcript_context`、`search_knowledge`、`query_action_audit` 和 `get_rule_detail` 等操作工具。两个 Agent 即使使用同一个模型供应商，也必须使用不同的上下文、Schema 和权限边界；双 Agent 本身不等于准确率自动提升。

### 6.11 增加 QC Agent Runtime 可视化控制台（新增；原确定性重放项已删除）

增加一个面向质检人员和开发调试的最小 Runtime 控制台，展示一次 Run 的结构化时间线和当前状态，体验上类似 Codex/Claude Code 的“能看到 Agent 做了什么”，但不展示模型隐藏思维链，也不把 UI 当作事实来源。

首版页面至少展示：

- 状态图当前节点、状态转换、轮数、重复调用检测结果和取消/审批状态；
- 执行 Agent 与评测 Agent 的角色、结构化输出、工具请求、参数摘要、校验结果和 Observation 摘要；
- 上下文各分区的来源、版本、长度和关键证据引用；
- QualityGate 的通过/拒绝、问题码、最终路由和人工复核入口；
- 副作用审批的批准/拒绝、参数哈希和结果确认状态。

首版优先复用现有 `GET /api/agent/runs/{run_id}` 作为读取入口做轮询查询，后续再考虑 SSE；但现有接口只有在整次 `analyze` 结束后才追加部分事件，Runtime 必须改为每个 Step Checkpoint 后持久化状态和事件，才能实现运行中的时间线。不要先建设完整监控大屏。取消、批准和拒绝按钮必须走服务端授权接口，前端隐藏按钮不能代替权限判断。原始敏感 Prompt、完整转写、密钥和未经脱敏的工具输出不得直接展示。

## 本阶段明确不做

- 不做多 Agent 群聊、自动角色生成和无限自主规划；只做固定职责的执行 Agent + 评测 Agent；
- 不为了框架名迁移现有可控 Loop；
- 不做运行 Lease、多 Worker 接管和并发推进保证；
- 不做跨步骤全局 Deadline、Token/费用 Runtime 预算、外部依赖熔断；
- 不做确定性重放、旧 Checkpoint 自动迁移和独立的失控测试矩阵；每项实现仍只补必要的定向验证，不把这些扩展写成已完成能力；
- 不开放 Shell、任意 SQL 和任意 URL 抓取工具；副作用协议先用可查询 Fake 验证；
- 不拆新的 LLM Gateway，不做多 Provider 自动降级平台；
- 不接真实 CRM 写接口，不建设完整观测大屏；控制台只做 Run 时间线、审批和取消的最小闭环。

## 可验证完成标准

- 一个持久化 `AgentState` 能明确记录当前节点、活动 Agent、计划、Observation、评测结论、上下文清单和 `stateVersion`；
- 进程在工具步骤后退出能够从最近 Checkpoint 恢复，且已提交的只读工具结果不会无故重复执行；
- 未注册工具、额外字段和越界资源在执行前被拒绝；执行 Agent 与评测 Agent 的工具集合和写权限不同；
- 取消请求能够阻止尚未开始的新动作，并留下 `CANCELLED` 或人工路由；
- 提高后的轮数上限仍然是硬上限；相同工具和规范化参数的无收益重复调用能够被检测并终止；
- 上下文分区、证据不可裁剪规则和来源/版本/长度元数据可在 Trace 或控制台查询；
- 审批暂停后能够从原 Checkpoint 批准或拒绝；Fake 副作用工具的超时未知结果不会被盲目重试；
- 控制台能够展示双 Agent 的结构化执行时间线、工具拒绝、评测结果、Gate 结果、取消和审批状态，并通过脱敏检查；
- Evaluator 和 Planner 均无法绕过最终 `QualityGate`；证据不足、重复循环或人工拒绝不会自动变成通过。

---

# 阶段 8：Agent 身份、权限与安全

## 审查结论

上一版把本阶段压缩成通用部署底线，判断过度。对 Agent 开发岗位，身份到工具权限的传递、最小能力授权、副作用审批、Prompt Injection 和数据外泄防护都具有很高辨识度，应恢复并做成可攻击、可验证的安全闭环。仍然删除 Kubernetes、多租户和服务网格，因为它们不能直接证明 Agent 权限模型是否正确。

## 选择升级的点

### 8.1 建立认证与业务 RBAC

使用短时 JWT Access Token 标识用户，密码只保存强哈希；公开部署时再增加 Refresh Token 撤销流程。保留 `ADMIN / RULE_MANAGER / REVIEWER / AUDITOR / VIEWER` 五个角色，但只实现与现有页面和接口有关的权限矩阵，不为了角色数量增加空接口。

RBAC 至少约束规则/知识发布、报告查看、音频访问、人工改判、批次取消、导出和审计查询。服务端执行权限判断，前端隐藏按钮不能作为授权。

### 8.2 把用户权限收敛为 Agent Capability

启动 run 时根据用户、任务目的、案件范围和工具风险生成不可由模型修改的 `AuthorizationContext`。它只包含本次运行需要的 capability，例如 `audit:read:case-123`，不会把用户完整角色或管理员 Token 暴露给 Prompt。

Planner 只能提出工具请求；Tool Executor 必须再次根据 capability、资源 ID、数据范围和工具版本授权，防止模型通过换参数访问其他案件。权限在长任务中被撤销时，下一步调用必须失败关闭。

### 8.3 对工具做风险分级与人工审批

工具分为只读、敏感读取和副作用三档。只读工具可自动执行；敏感读取要求相应数据权限；副作用工具必须进入阶段 6 的 `WAITING_APPROVAL`。审批令牌绑定 run、tool、参数哈希、审批人和过期时间，只能消费一次，不能挪给另一个调用。

### 8.4 防止 Prompt Injection 改写权限

System Policy、工具定义和授权上下文属于可信控制面；转写、知识文档、CRM 返回和用户备注全部视为不可信数据。检索内容使用明确数据边界包裹，文档中的“忽略规则”“调用某工具”不得进入控制指令。

增加直接注入、间接知识库注入、伪造工具结果和诱导泄密用例。安全目标不是检测所有恶意文字，而是即使模型受诱导，Executor 和 Gate 仍能阻止越权动作与错误自动落地。

### 8.5 限制工具的数据外泄通道

禁止任意 URL、Shell、原始 SQL 和不受控文件路径；HTTP 工具使用域名白名单、请求方法限制、响应大小限制和重定向检查，文件工具使用规范化路径与项目内允许目录。工具输出在进入模型前进行字段白名单、长度限制和敏感信息脱敏。

### 8.6 保护音频、报告、Prompt 与密钥

音频与报告按资源权限读取，下载使用短期签名 URL 或受控流式接口；数据库和日志不保存明文密码、Authorization、完整 Prompt 和未脱敏客户信息。模型 API Key 只注入 Gateway，不进入 AgentState、Checkpoint 或工具参数。

定义敏感字段分级、保存期限和删除接口。删除持久业务数据仍需明确授权和审计，不能把模型权重或证据快照当普通临时缓存清理。

### 8.7 将限流与 Agent 预算联动

登录按账号和 IP 限制，创建任务、下载音频、导出和高成本模型接口按用户限额；单 Run 沿用 Runtime 已实现的轮数和工具次数上限，Token/费用只做账本记录，不在本阶段充当新的授权预算。限流或轮数/工具次数耗尽返回结构化原因，不自动切换到权限更宽或成本更高的模型。

### 8.8 建立不可抵赖的安全审计链

记录 actor、role、runId、tool、resource、decision、policyVersion、参数哈希、审批信息、结果和 traceId。重点审计规则/知识发布、人工改判、敏感音频访问、权限变更、拒绝的工具调用和副作用执行。

审计记录采用追加写和严格访问控制，业务 API 不提供原地修改。求职版不必实现区块链或外部 WORM 存储，但要能检测普通篡改并保留前后关联。

### 8.9 建立 Agent 安全对抗测试集

测试集覆盖水平越权、垂直越权、审批令牌重放、知识库注入、工具参数走私、任意 URL、路径穿越、敏感字段回显、日志泄密和权限中途撤销。每个案例同时断言工具未执行、终态失败关闭、审计存在且响应不泄露内部细节。

### 8.10 提供最小 Docker Compose

Compose 只编排 API、Worker、PostgreSQL 和 Redis；如阶段 4 选择外部 Trace 后端，再作为可选 profile。模型目录使用只读挂载或明确的持久卷，启动时不得隐式重新下载模型。服务提供健康检查、依赖就绪判断和优雅停机。

## 本阶段明确不做

- 不做 Kubernetes、Helm、服务网格或正式发布流水线；
- 不做多租户、ABAC 策略平台和外部 OAuth/OIDC 身份集成；
- 不做 Nginx + Redis 的复杂分布式限流；
- 不接真实 CRM 写接口，先用可查询 Fake Tool 验证副作用授权与审计；
- 不声称通过自建对抗用例就完成了专业渗透测试或安全合规认证；
- 不声称本地 Compose 等同生产部署或已经完成灾备。

## 可验证完成标准

- 未登录请求不能访问业务 API，五个角色的已实现权限矩阵有参数化测试；
- 相同工具在不同用户、案件范围和 capability 下得到正确的允许或拒绝结果；
- 副作用工具没有单次审批令牌时不能执行，重复消费令牌不会产生第二次动作；
- Prompt Injection、越权、SSRF、路径穿越和日志泄密对抗用例失败关闭并产生审计；
- 权限中途撤销后，长任务下一次工具调用被拒绝且不能沿用旧授权；
- 新环境可以通过 Compose 启动 API、Worker、PostgreSQL 和 Redis；
- 容器停止时不接新任务，正在执行的任务可恢复；

---

# 四、最终应该形成的作品集主线

八个实施阶段完成后，不要把项目讲成“我搭了一套大而全的平台”，而应围绕四个问题组织演示和面试叙事。

## 4.1 非确定性怎样被约束

LLM 只负责结构化事件、执行 Agent 的候选动作和评测 Agent 的证据判断；工具通过 Registry、Schema、权限和有限轮数受控；规则、扣分、状态转换和最终 Gate 保持确定性；不确定结果进入人工复核。

## 4.2 长任务怎样可靠执行

API 与 Worker 解耦，PostgreSQL Outbox 解决任务入库与投递的故障窗口，Redis Streams 提供至少一次投递，阶段产物 Checkpoint 与业务幂等保证恢复后不重复高成本步骤或生成重复报告。

## 4.3 优化是否有证据

模型、Prompt、规则、知识、工具契约和双 Agent 架构都进入评测清单；任何 RAG 或 Agent 改动用同一数据集比较；失败案例、工具轨迹和历史引用可以查询；Trace 与成本账本解释线上式故障和调用开销，但不把查询能力描述成确定性重放。

## 4.4 Agent 为什么不能越权

用户角色只在控制面表达业务权限，Runtime 将其收敛为本次 run 的最小 capability；Planner 只能提出工具请求，Executor 依据 capability、资源范围和风险级别再次授权；副作用工具需要单次审批令牌；不可信转写和知识内容无法改变权限与 Gate。

## 4.5 建议的完成证据包

求职前至少准备以下材料，而不只是准备代码截图：

1. 一次 `report=None` 或 Fail Open 的测试先失败、修复后通过的提交记录；
2. 一次 Worker 在 ASR 后退出并从产物恢复的录屏或日志；
3. 一次 Redis 重复投递但只产生一份有效报告的测试；
4. 一份两个 Prompt 或检索版本的评测 Diff，并解释一个真实退化案例；
5. 一次未知工具、重复调用终止、取消或 Prompt Injection 被拒绝的双 Agent Trace；
6. 一次模型不确定结果被人工确认或改判，原始报告不变且当前有效结果正确切换的演示；
7. 一组相同工具在不同角色、资源范围和审批状态下被允许或拒绝的权限测试；
8. 一次 Prompt Injection、审批令牌重放或数据外泄攻击被阻断并留下审计的演示；

如果时间不足，可以删掉阶段 4 的外部 Trace 后端和阶段 8 的公开部署，但不能再删阶段 4 的本地 Trace/日志证据、Agent capability、工具二次鉴权、审批令牌和阶段 6 的双 Agent 状态/Checkpoint 证据；这些共同构成 Agent Runtime 的可信执行边界。

---

# 五、升级完成后的简历项目描述候选稿

## 使用前提

以下文案使用完成时态，只能在对应实现、测试和可复现证据已经存在后使用。当前仍为规划的功能应删除对应 bullet，不能把本蓝图本身当作项目成果。

## 1. 项目名称

证据驱动的催收通话智能质检 Agent

## 2. 一句话项目简介

面向催收通话的可恢复质检工作流，将真实语音转写、RAG 证据检索、有界 Agent 决策、确定性门禁与人工复核串成可评测、可追溯闭环。

## 3. 精简技术栈

Python、FastAPI、PostgreSQL、Redis Streams、DeepSeek、FunASR、BGE、JWT/RBAC、OpenTelemetry、pytest、Docker Compose

## 4. 十六条宽口径简历项目 bullet 候选

下面 16 条是升级完成后的宽口径候选池，不再为了固定字数牺牲信息完整性。每条严格按照“具体工程问题 → 关键技术 → 工程方案 → 测试或查询验证结果”组织；最终根据目标岗位挑选 4～5 条，并只保留已经真实实现且有证据支撑的内容。

- 针对大模型结构化输出缺字段、伪造证据或规则计算错误仍可能被自动放行的问题，基于 Pydantic 领域模型与确定性 QualityGate，设计 Direct/Agent 双路径共用的事件引用、规则有效期、RAG 支撑和分数复算门禁；经非法输出、证据缺失及 Gate 绕过用例验证，实现异常报告统一转人工复核或失败关闭。
- 针对音频转码、ASR 和 Agent 质检耗时较长、同步接口易被阻塞且运行状态难追踪的问题，基于 FastAPI、PostgreSQL 与独立 Worker，设计 `202 Accepted + jobId` 异步控制面、三级任务状态及历史运行查询；经接口契约和长任务隔离用例验证，实现 API 快速返回、任务后台推进与结果持续可查。
- 针对任务已经写入数据库但消息尚未投递时服务崩溃，以及 Redis Streams 至少一次投递可能重复生成报告的问题，基于 PostgreSQL Transactional Outbox、Consumer Group 与业务唯一约束，设计业务数据和事件同事务提交、成功持久化后 ACK、条件状态更新的幂等消费链路；经 Publisher 中断、消息重投和重复消费用例验证，实现未发布任务可补发且同一运行只保留一份有效报告。
- 针对批量恢复时重复执行高成本 ASR、损坏音频阻塞整批任务以及模型按文件反复加载的问题，基于 FunASR、常驻模型 Worker 与阶段 Checkpoint，设计 WAV/Transcript 产物哈希、模型版本记录、有效产物复用和阶段级失败隔离；经 Worker 在 ASR 后中断、坏文件注入及调用次数断言验证，实现已完成 ASR 不重跑、异常文件独立失败且其余任务继续执行。
- 针对 DeepSeek 超时、限流、服务异常或非法 JSON 可能污染质检结果并造成调用不可追溯的问题，基于统一代码级 LLM Gateway，设计错误分类、有限重试、Pydantic 结构化输出校验、模型与 Prompt 版本记录及 Token 用量采集；经 429、超时、5xx 和非法响应用例验证，实现可恢复错误按策略重试、无效输出失败关闭且每次模型调用可追溯。
- 针对 Agent 进程崩溃丢失执行状态、Planner 与 Evaluator 权限混杂以及当前 Loop 难以查询执行位置的问题，基于显式状态机、共享类型化 `AgentState`、原子 Step Checkpoint、取消传播和固定职责双 Agent，设计 `ROUTE/EXEC_PLAN/AUTHORIZE/ACT/OBSERVE/EVALUATE/GATE` 状态流转与确定性 reducer；经进程中断、取消请求和 Evaluator 试图绕过 Gate 的用例验证，实现从最近提交点恢复、评测权限隔离且最终业务结果仍由 Gate 控制。
- 针对 Agent 重复调用同一工具和上下文持续膨胀导致无收益循环与输入噪声的问题，基于提高但有硬上限的轮数、同参调用指纹、证据窗口和分区上下文工程，设计不可裁剪证据/规则区、Observation 摘要和无新增证据终止策略；经超长上下文、重复调用和轮数耗尽用例验证，实现关键引用不被裁剪、无收益循环被终止且不确定结果转人工复核。不得把本 bullet 写成已实现全局 Deadline、费用预算或熔断。
- 针对 Planner 编造不存在的工具、篡改参数或借合法用户身份跨案件读取数据的问题，基于类型化 Tool Registry、Pydantic Schema、按 Agent 分配的 capability 与 Executor 二次鉴权，设计工具白名单、参数/输出校验、资源范围绑定和风险分级；经未知工具、额外字段、水平越权、垂直越权及权限中途撤销用例验证，实现非法请求在工具执行前被拒绝且拒绝原因进入审计轨迹。
- 针对副作用工具超时后结果未知、盲目重试可能重复写入以及人工拒绝可被重新发起 Run 绕过的问题，基于风险分级、单次审批令牌、幂等键与 Human Interrupt/Resume，设计 `WAITING_APPROVAL` Checkpoint、参数哈希绑定、结果查询确认和拒绝状态继承协议；经审批令牌重放、调用超时、重复消息和拒绝后重跑用例验证，实现高风险动作未经授权无法执行、已生效动作不会重复写入且人工拒绝不可绕过。
- 针对质检人员无法理解 Agent 为什么查证据、何时被评测 Agent 否决以及如何接管长任务的问题，基于 Runtime 状态时间线和脱敏控制台，设计双 Agent 角色、结构化计划、工具请求/拒绝、Observation、上下文分区、Gate 和审批状态的可视化查询与取消/审批入口；经状态切换、权限拒绝、人工取消和敏感字段脱敏用例验证，实现一次 Run 的关键执行过程可观察、可解释、可人工接管。
- 针对纯语义检索漏掉规则编号、过期规则或恶意知识文档被高相似度召回并错误支持自动处罚的问题，基于 Dense/BM25 Hybrid Search、不可变知识版本与历史证据快照，设计 RRF 融合、事件类型/规则关系/生效时间硬过滤、引用内容哈希和 Prompt Injection 隔离；经 Dense-only 对照、过期文档、错事件类型及恶意 Chunk 用例验证，实现目标检索配置可量化比较、不合格证据无法进入处罚链且历史报告仍可还原当时依据。
- 针对模型、Prompt、规则或知识变更后效果静默退化且失败原因无法定位的问题，基于可复现离线评测、OpenTelemetry Trace 与结构化日志，设计最小实验 Manifest、事件/违规/RAG 四维指标、确定性硬判断与 LLM Judge 分层评分、两版本 Diff 和失败案例包；经漏检、错误引用、Judge 分歧及敏感字段用例验证，实现退化案例可关联运行链路定位，且 Judge 不能覆盖确定性失败。
- 针对不确定报告缺少正式人工承接、人工改判直接覆盖模型原报告以及并发提交互相覆盖的问题，基于不可变修订快照、乐观锁、提交幂等键与结构化理由码，设计复核任务自动路由、原始报告/审核修订分离和当前有效结果引用；经双请求并发提交、网络重试及退回重跑用例验证，实现待审核结果不会提前生效、原始与修订结果同时保留且重复提交不产生多份修订。
- 针对普通用户访问敏感音频、审核员越权发布规则或不同角色调用高风险接口的问题，基于短时 JWT 与 `ADMIN/RULE_MANAGER/REVIEWER/AUDITOR/VIEWER` 五角色 RBAC，设计规则发布、报告查看、音频访问、人工改判、批次取消和审计查询的资源级权限矩阵；经角色参数化、水平越权和垂直越权用例验证，实现未登录请求失败关闭、业务接口按角色与资源授权且权限决策可审计。
- 针对转写、知识文档或工具结果中的 Prompt Injection 诱导 Agent 越权调用、SSRF 访问内网及敏感字段回流模型的问题，基于可信控制面/不可信数据分区、域名白名单、路径规范化与输出字段白名单，设计指令隔离、请求方法/重定向/响应大小限制和进入模型前脱敏；经直接注入、间接知识库注入、任意 URL、路径穿越和敏感字段回显对抗用例验证，实现恶意内容无法改变 Capability、未授权外联被阻断且失败过程留下安全审计。
- 针对批量任务失败后难以定位具体阶段、权限拒绝缺少上下文以及 Token 与费用来源无法核对的问题，基于统一 `traceId/runId/batchId/itemId`、OpenTelemetry Span、结构化日志、追加式安全审计和模型用量账本，设计从 API、Outbox、Redis、Worker、Agent Step、工具审批到 QualityGate 的关联链路；经 runId 关联查询、LLM 429、工具超时、重复调用终止和日志脱敏用例验证，实现单次运行的执行步骤、授权决策、重试原因、Token 来源及估算费用可追溯。

## 5. 候选 bullet 的证据门槛

| 候选主题 | 写入简历前至少应具备的证据 |
|---|---|
| 可信质检 | Fail Open 失败测试、Gate 问题码、人工/失败终态断言 |
| 批量与异步 | 真实音频显式 E2E、Outbox 事务测试、重复消息、坏文件隔离与中断恢复记录 |
| LLM Gateway | 非法输出、超时、分类重试、模型与 Prompt 版本及 Token 来源测试 |
| Agent Runtime | 双 Agent 状态图、Tool Registry、Checkpoint、取消传播、重复调用终止、上下文清单和控制台查询证据 |
| 权限与副作用 | 五角色权限矩阵、Capability 二次鉴权、单次审批令牌和幂等副作用测试 |
| 评测与 RAG | 数据集哈希与 Manifest、两个版本 Diff、RAG 四项指标、Judge/确定性分歧案例和历史证据快照 |
| 人工复核与安全 | 原始/修订快照、并发提交、Prompt Injection、越权和脱敏回归测试 |
| 可观测与审计 | 关联 Trace、追加式审计记录、日志脱敏和 Token/费用来源记录 |

## 6. 面试表达边界

- 可以说“基于 Redis Streams 的至少一次投递和业务幂等”，不要说“实现严格 Exactly Once”；
- 可以说“本地 Compose 可复现”，不要说“达到生产级高可用”；
- 可以说“费用按版本化价格配置估算”，不要把估算值说成 Provider 账单；
- 可以说“单声道录音使用说话人聚类和启发式角色映射”，不要说“准确识别坐席与客户”；
- Mock 审计服务必须明确称为 Mock，不得表述为已经对接真实 CRM；
- 没有独立人工金标和足够样本时，不宣称模型准确率、泛化能力或业务收益；
- 若某阶段尚未完成，直接删掉相应技术栈和 bullet，不用“设计了”“规划了”伪装实现。

## 六、最终取舍结论

这个项目真正值得拉开差距的不是技术栈数量，而是五件事：失败时不会错误自动通过，长任务中断后不会从头重复，双 Agent 的状态和权限不能失控，模型提出的工具调用不能越权，每次优化都能用离线评测、运行 Trace 与失败案例证明。

因此，最合理的求职版范围是：用最小 FastAPI/PostgreSQL 后端承载共享 `AgentState`，以一个 Redis Streams Worker 跑通真实音频，以固定职责双 Agent 和 Human-in-the-loop 承接不确定结果；重点投入阶段恢复、Tool Registry、上下文工程、Capability/工具安全、RAG 证据和离线评测，Runtime 控制台只做到可定位、可解释、不过度承诺。Lease、全局 Deadline、确定性重放和版本迁移不写入本阶段完成成果。
